The hero composite-index dial — an animated clay progress ring around a huge italic serif numeral. The focal point of any overview screen.

```jsx
<ScoreRing value={94} label="Points" size={256} />
```

Props: `value`, `max` (default 100), `size`, `stroke`, `label`, `color`. The arc animates from empty on mount.
