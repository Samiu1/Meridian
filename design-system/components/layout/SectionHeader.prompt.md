An eyebrow label + hairline divider that opens each dashboard section.

```jsx
<SectionHeader>Recovery Symphony</SectionHeader>
```

Props: `align` ("left" | "right"). The wide-tracked uppercase label sits on the chosen side; the thin clay rule fills the rest of the row.
