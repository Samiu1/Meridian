# Anthropic — required. The Agent SDK reads this automatically.
ANTHROPIC_API_KEY=

# Agent defaults
PM_AGENT_MODEL=claude-sonnet-4-6
PM_AGENT_MAX_TURNS=30

# API
PORT=8787

# Clerk (not wired yet)
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=

# Postgres (not wired yet — sessions are JSONL files in v1)
DATABASE_URL=
